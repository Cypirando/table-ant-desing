// import { Input } from 'antd';

// function handleChange(e, field, id) {
//   // lógica para lidar com a alteração do valor do campo
// }

// const EditableTableColumn = ({ text, record }) => {
//   return (
//     <>
//       {record.editable ? (
//         <Input defaultValue={text} onChange={e => handleChange(e, 'name', record.id)} />
//       ) : (
//         text
//       )}
//     </>
//   );
// };

// export default EditableTableColumn;